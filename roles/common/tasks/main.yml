---
# tasks file for roles/common
- name: Atualizar apt e instalar pacotes básicos
  apt:
    name:
      - curl
      - wget
      - git
      - joe
      - htop
      - ncdu
      - net-tools
      - dnsutils
      - nmap
      - tree
      - unzip
      - build-essential

    state: present
    update_cache: yes
